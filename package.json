{
    "name": "@fourze/monorepo",
    "private": true,
    "version": "0.8.0",
    "description": "a mock framework",
    "files": [
        "dist/*"
    ],
    "keywords": [
        "mock",
        "web",
        "vite",
        "hmr"
    ],
    "scripts": {
        "build": "rimraf packages/*/dist && pnpm -r --filter=./packages/* build",
        "stub": "pnpm -r --filter=./packages/* --parallel run stub",
        "play:vite": "pnpm build &&  pnpm -r --filter=./playgrounds/vite dev",
        "play:vue-cli": "pnpm build &&  pnpm -r --filter=./playgrounds/vue-cli dev",
        "play:server": "pnpm -r --filter=playgrounds-server dev",
        "test": "echo \"Error: no test specified\" && exit 1",
        "bump": "bumpp package.json packages/*/package.json --commit --push --tag",
        "preinstall": "npx only-allow pnpm",
        "postinstall": "husky install"
    },
    "author": "chizuki",
    "license": "MIT",
    "lint-staged": {
        "*.{js,jsx,ts,tsx}": [
            "prettier --write"
        ]
    },
    "devDependencies": {
        "@types/node": "^18.6.0",
        "bumpp": "^8.2.1",
        "chokidar": "^3.5.3",
        "esno": "^0.16.3",
        "husky": "^8.0.1",
        "lint-staged": "^13.0.3",
        "prettier": "^2.7.1",
        "rimraf": "^3.0.2",
        "typescript": "^4.7.4",
        "unbuild": "^0.7.6"
    }
}
